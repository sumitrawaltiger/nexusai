[project]
name = "03-agentic-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sumit Rawal", email = "sumit.rawal@txtgroup.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
03-agentic-ai = "03_agentic_ai:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
